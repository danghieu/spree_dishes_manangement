<%= form_for(@available_on) do |f| %>
  <% if @available_on.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@available_on.errors.count, "error") %> prohibited this available_on from being saved:</h2>

      <ul>
      <% @available_on.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
